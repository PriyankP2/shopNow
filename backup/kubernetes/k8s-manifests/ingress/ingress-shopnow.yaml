apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: shopnow-ingress
  namespace: shopnow-priyankp2
  annotations:
    nginx.ingress.kubernetes.io/use-regex: "true"
spec:
  ingressClassName: nginx
  rules:
  - http:
      paths:
      - path: /priyankp2-admin
        pathType: Prefix
        backend:
          service:
            name: admin
            port:
              number: 80
      - path: /priyankp2
        pathType: Prefix
        backend:
          service:
            name: frontend
            port:
              number: 80
